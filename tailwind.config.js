/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./*.html"], // Scans your HTML files
    theme: {
      extend: {}, // Add custom colors or sizes here if you want later
    },
    plugins: [],
  }